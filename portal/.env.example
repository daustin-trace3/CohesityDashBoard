# ICC Portal environment
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
PORT=3100
# Optional override; defaults to backend/data/portal.db
# PORTAL_DB_PATH=
